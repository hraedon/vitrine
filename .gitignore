# environments / build artifacts
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# rendered site — a projection, never committed
_site/

# persistent advisory locks used to serialize local publication writers
.*.publish.lock

# downloaded raw source material — cite it, don't commit it
samples/

# runtime data
*.sqlite3
*.db

# identifier gate local denylist — never committed
.identifiers-denylist.local
room-1950s-full.png
