"""vitrine — a decade-by-decade museum of the median family's lifestyle.

Deterministic, stdlib-only core: the fact model, the loader, and the
provenance gate. Rendering lives in ``vitrine.site`` (the ``[site]`` extra)
and imports this core, never the reverse.
"""

__version__ = "0.1.0"
